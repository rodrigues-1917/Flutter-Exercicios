git init

git add "nome_Arquivo"

git commit -m "criando alteração ...."

git remote add origin <http.......com>

git config --global user.name ""

git config --global user.email ""

git push origin master





git add * 

git commit -m "o que foi feito"

git push origin master


git pull origin master




Comando para forçar pull e sobrescrever arquivos locais do repositório
git fetch
git reset --hard HEAD
git merge origin/nome_do_branch



git branch -m master main


